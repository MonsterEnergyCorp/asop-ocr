apiVersion: v1
kind: Service
metadata:
  name: {{ include "archival.fullname" . }}
  labels:
    {{- include "archival.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "archival.selectorLabels" . | nindent 4 }}
