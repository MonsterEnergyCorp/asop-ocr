apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocr-ingest-documents.fullname" . }}-test-connection"
  labels:
    {{- include "ocr-ingest-documents.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ocr-ingest-documents.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
