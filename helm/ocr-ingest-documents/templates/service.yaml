apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocr-ingest-documents.fullname" . }}
  labels:
    {{- include "ocr-ingest-documents.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocr-ingest-documents.selectorLabels" . | nindent 4 }}
