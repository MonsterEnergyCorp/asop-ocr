apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocr-erp-push.fullname" . }}
  labels:
    {{- include "ocr-erp-push.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocr-erp-push.selectorLabels" . | nindent 4 }}
