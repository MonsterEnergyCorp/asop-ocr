apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocr-pdf-parser.fullname" . }}
  labels:
    {{- include "ocr-pdf-parser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocr-pdf-parser.selectorLabels" . | nindent 4 }}
