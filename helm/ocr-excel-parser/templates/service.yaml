apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocr-excel-parser.fullname" . }}
  labels:
    {{- include "ocr-excel-parser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocr-excel-parser.selectorLabels" . | nindent 4 }}
