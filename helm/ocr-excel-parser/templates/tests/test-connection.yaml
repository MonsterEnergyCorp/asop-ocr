apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocr-excel-parser.fullname" . }}-test-connection"
  labels:
    {{- include "ocr-excel-parser.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ocr-excel-parser.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
